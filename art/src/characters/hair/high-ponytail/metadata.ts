import { msg } from "@lingui/core/macro";
import type { ItemMetadata } from "../../../types.ts";

export default {
  id: "high-ponytail",
  name: msg`High ponytail`,
} as const satisfies ItemMetadata;
