import type { CombatIconMetadata } from "../../../../types.ts";

export default {
  id: "icon-status-speed-up",
  category: "status",
  stat: "speed",
  direction: "up",
} as const satisfies CombatIconMetadata;
